spring:
  application:
    name: trade-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        ip: ${HMALL_NACOS_IP:127.0.0.1}
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-JDBC.yaml
          - data-id: Shared-log.yaml
          - data-id: shared-swagger.yaml
          - data-id: shared-seata.yaml
          - data-id: shared-mq.yaml
