spring:
  application:
    name: gateway # 微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        ip: ${HMALL_NACOS_IP:127.0.0.1}
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml
